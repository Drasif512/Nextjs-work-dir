export type ButtonPropes = {
    title:string
    data? : string
    contact? : number
}

export type CardPropes = {
    title:string,
    description? : String,
    tag? : string,
    isTitle? : boolean,
    isDescription? : boolean,
    isTag? : boolean,
    isBc? : boolean,
    style? : {},
}